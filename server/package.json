{
  "name": "captureordie-server",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "Backend for CaptureOrDie: MyAnimeList OAuth + proxied MAL API. The MAL client secret lives ONLY here, never in the browser bundle.",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "node --watch index.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "cookie-parser": "^1.4.7",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.21.1",
    "express-rate-limit": "^7.4.1",
    "helmet": "^8.0.0"
  }
}
